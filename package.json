{
  "name": "chooselife-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "deploy:functions": "./deploy-functions.sh"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "turbo": "^2.6.1"
  },
  "packageManager": "pnpm@10.26.0"
}
