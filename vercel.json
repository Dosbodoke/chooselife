{
  "version": 2,
  "ignoreCommand": "if ! git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA^{commit}\" 2>/dev/null; then echo \"✅ - Proceeding with build (previous SHA invalid or first deployment).\"; exit 1; fi; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- ./next"
}
