import { TabExperience } from './shared';

/** A screen-local bottom dock puts section switching within thumb reach. */
export default function Dock() {
  return <TabExperience direction="dock" />;
}
