import { TabExperience } from './shared';

/** Visible cover and persistent secondary tabs prioritize orientation. */
export default function Sticky() {
  return <TabExperience direction="sticky" />;
}
